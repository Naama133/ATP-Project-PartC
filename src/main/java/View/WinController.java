package View;import javafx.fxml.Initializable;import javafx.scene.control.Button;import javafx.scene.image.Image;import javafx.scene.image.ImageView;import java.io.InputStream;import java.net.URL;import java.util.ResourceBundle;/** * WinController - controller of window stage */public class WinController implements Initializable {    public Button Restart; //restart game option    public Button Exit; //exit game option    public ImageView win;    @Override    public void initialize(URL url, ResourceBundle resourceBundle) {        win.setImage(new Image(getImageResources("images/winning.jpg")));    }    //return images    private InputStream getImageResources(String path){        return (getClass().getClassLoader().getResourceAsStream(path));    }}